<v-container fluid>
  <v-layout row wrap>
    <filter-form entrytype="revenue"></filter-form>

    <Entries userid="<%= current_user.id %>" entrytype="revenue"></Entries>
    <%= render 'table' %>
  </v-layout>
</v-container>

<%= content_for :css do %>
  <%= stylesheet_link_tag 'entry_index', media: 'all' %>
  <%= stylesheet_link_tag 'table', media: 'all' %>
<% end %>

<div class="container">
  <h2>Receitas</h2>

  <table>
    <thead>
      <tr>
        <th>Descrição</th>
        <th>Valor</th>
        <th>Data</th>
      </tr>
    </thead>

    <tbody>
      <% @revenues.each do |revenue| %>
        <tr class="entry">
          <%= content_tag(:td, revenue.description, class: 'description') %>
          <%= content_tag(:td, number_to_currency(revenue.amount), class: 'amount') %>
          <%= content_tag(:td, revenue.entry_date.strftime('%d/%m/%Y'), class: 'entry-date') %>
        </tr>
      <% end %>
    </tbody>
  </table>
</div>

<%= form_tag '/recover' do |f| %>
  <div class="row">
    <div class="logo col s6 offset-s3 center-align">
      <image src="<%= image_url('million-logo.svg') %>">
      <h4>Recuperar senha</h4>
    </div>

    <div class="input-field col s12">
      <%= content_tag :label, 'Digite o email cadastrado', for: 'email' %>
      <%= email_field_tag :email, '' %>
    </div>

    <div class="col s12 center-align">
      <%= submit_tag 'Envie', class: 'btn waves-effect waves-light btn-small' %>

      <p>
      Se lembrou da senha?<br>
      <%= link_to 'Entre aqui', login_path %>
      ou
      <%= link_to 'volte ao início', root_path %>
      </p>
    </div>
  </div>
<% end %>
